# Workspace Cleanup Plan

## Current State Summary

| Category | Item | Details |
|---|---|---|
| **Modified files** | 20 files | +2185 / -797 lines of uncommitted work on `main` |
| **Untracked files** | `.ha_snapshots/` | Home Assistant JSON snapshots (~1.4 MB), unrelated to this project |
| | `00_READ_FIRST.md` | Home Assistant operations runbook, unrelated to this project |
| | `AGENTS.md` | AI assistant instructions (project-relevant) |
| | `docker-compose.pi.yaml` | Pi-specific Docker Compose overlay (project-relevant) |
| **Stale branches** | 8 `claude/*` branches | Only `claude/dreamy-dirac-a04b4a` has 4 unique commits; the other 7 are at or behind `main` |
| **Stash** | 1 stash | `WIP on main: 4cc4055` (old, likely obsolete) |
| **Security concern** | `deploy.sh` diff | Hardcoded Pi IP (`192.168.68.53`) replacing placeholder — should not be committed |

## Cleanup Steps

### Step 1: Remove unrelated files
- Delete `.ha_snapshots/` directory (Home Assistant data, not part of kick-api)
- Delete `00_READ_FIRST.md` (Home Assistant runbook, not part of kick-api)

### Step 2: Update `.gitignore`
Add entries to prevent future contamination:
```
# Home Assistant artifacts
.ha_snapshots/
00_READ_FIRST.md
```

### Step 3: Fix `deploy.sh` security issue
The current diff hardcodes `192.168.68.53` as the default PI_HOST. This IP should not be committed to the repo. Revert the IP-specific defaults back to generic placeholders, but keep the structural improvements (compose env prefix, `run_remote_compose` helper, Chromecast network check, `docker-compose.pi.yaml` integration). The correct approach: keep `PI_HOST` with no default (require it from env file or argument), don't embed real IPs.

### Step 4: Stage and commit project-relevant untracked files
- `AGENTS.md` — add and commit (project documentation for AI assistants)
- `docker-compose.pi.yaml` — add and commit (Pi deployment config, referenced by `deploy.sh`)

### Step 5: Commit the working tree changes
All 20 modified files contain project improvements (Typesense key warmup, shared video player, responsive layout, Safari fixes, search caching, etc.). After fixing `deploy.sh` in Step 3, stage everything and create a single commit. Suggested message style following repo convention:

```
Add shared-video mini-player, responsive layout overhaul, Safari fixes, search caching, Pi compose overlay, and Typesense warm-up
```

### Step 6: Delete stale `claude/*` branches
Remove the 7 branches that have no unique commits:
- `claude/amazing-germain-c5555d`
- `claude/gallant-euclid-b5e3c8`
- `claude/magical-hypatia-f64bb7`
- `claude/romantic-spence-85b19f`
- `claude/sad-proskuriakova-8a60ad`
- `claude/unruffled-easley-88ea02`
- `claude/vigorous-villani-2dc0bb`

Delete `claude/dreamy-dirac-a04b4a` too — its changes are already in the working tree and will be committed to `main`. **User confirmed.**

### Step 7: Drop the stale stash
```
git stash drop stash@{0}
```
The stash is from an old commit (`4cc4055`) and the working tree already contains all the evolved changes.

## Execution Order
1 → 2 → 3 → 4 → 5 → 6 → 7

Steps 1-3 are preparatory. Step 5 is the main commit. Steps 6-7 are branch/stash hygiene.
